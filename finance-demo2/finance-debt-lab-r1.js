(()=>{
  if(window.__PAVENRO_DEBT_LAB_R1__) return;
  window.__PAVENRO_DEBT_LAB_R1__=true;

  const STORAGE='pavenro.finance.debts.r1';
  const UIKEY='pavenro.finance.debt.ui.r1';
  const DEFAULT_DEBTS=[
    {id:'db1',name:'HDFC Credit Card',type:'Credit Card',balance:45320,apr:34.8,minPayment:2800,payment:5500,dueDay:7},
    {id:'db2',name:'Car Loan',type:'Vehicle Loan',balance:338000,apr:9.2,minPayment:0,payment:10850,dueDay:12},
    {id:'db3',name:'Personal Loan',type:'Personal Loan',balance:118000,apr:13.5,minPayment:0,payment:6200,dueDay:18}
  ];

  const state={tab:'overview',selected:'db1',extra:0,lump:0,calc:'emi'};
  try{Object.assign(state,JSON.parse(localStorage.getItem(UIKEY)||'{}')||{})}catch(e){}
  let debts=loadDebts();

  const style=document.createElement('style');
  style.id='pavenro-debt-lab-r1-style';
  style.textContent=`
    .pv-debt{height:100%;min-height:0;display:grid;grid-template-rows:auto auto minmax(0,1fr);gap:8px;color:var(--text,#17301f)}
    .pv-debt *{box-sizing:border-box}.pv-debt button,.pv-debt input,.pv-debt select{font:inherit}
    .pv-debt-head{display:flex;align-items:center;gap:10px;min-height:38px}.pv-debt-title{min-width:0}.pv-debt-title h2{margin:0;font:700 22px/1.05 Georgia,serif}.pv-debt-title p{margin:3px 0 0;color:var(--muted,#68756c);font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    .pv-debt-head-actions{margin-left:auto;display:flex;gap:6px}.pv-dbtn{height:31px;border:1px solid var(--border,#d8e1da);background:var(--panel,#fff);color:inherit;border-radius:9px;padding:0 10px;font-weight:750;font-size:10px;cursor:pointer}.pv-dbtn.primary{background:var(--brand,#276340);color:white;border-color:transparent}.pv-dbtn.soft{background:var(--panel2,#eef3ed)}.pv-dbtn:disabled{opacity:.5;cursor:not-allowed}
    .pv-debt-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:7px}.pv-dkpi{background:var(--panel,#fff);border:1px solid var(--border,#dfe6e0);border-radius:11px;padding:7px 9px;min-width:0}.pv-dkpi span{display:block;color:var(--muted,#68756c);font-size:8.5px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pv-dkpi b{display:block;margin-top:3px;font:800 15px/1.05 Georgia,serif;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pv-dkpi small{display:block;color:var(--muted,#68756c);font-size:8px;margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    .pv-debt-body{min-height:0;display:grid;grid-template-rows:36px minmax(0,1fr);background:var(--panel,#fff);border:1px solid var(--border,#dfe6e0);border-radius:12px;overflow:hidden}.pv-dtabs{display:flex;gap:2px;padding:4px 6px;border-bottom:1px solid var(--border,#e3e9e4);background:var(--panel2,#f4f7f3)}.pv-dtab{border:0;background:transparent;color:var(--muted,#68756c);border-radius:8px;padding:0 11px;font-size:9.5px;font-weight:800;cursor:pointer}.pv-dtab.active{background:var(--panel,#fff);color:var(--text,#17301f);box-shadow:0 0 0 1px var(--border,#dfe6e0)}.pv-dpane{min-height:0;overflow:auto;padding:8px;scrollbar-width:thin}
    .pv-dgrid2{height:100%;min-height:0;display:grid;grid-template-columns:minmax(0,1.32fr) minmax(245px,.68fr);gap:8px}.pv-dcard{min-height:0;background:var(--panel2,#f5f7f4);border:1px solid var(--border,#dfe6e0);border-radius:10px;padding:8px;overflow:auto}.pv-dcard h3{font-size:11px;margin:0 0 6px}.pv-debt-list{display:grid;gap:5px}.pv-debt-row{display:grid;grid-template-columns:minmax(0,1.35fr) .8fr .72fr .72fr 62px;gap:7px;align-items:center;padding:7px 8px;border-radius:9px;background:var(--panel,#fff);border:1px solid transparent;cursor:pointer}.pv-debt-row:hover,.pv-debt-row.active{border-color:color-mix(in srgb,var(--brand,#276340) 38%,transparent)}.pv-debt-row .name{min-width:0}.pv-debt-row .name b,.pv-debt-row .name small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pv-debt-row .name b{font-size:10px}.pv-debt-row small,.pv-debt-row label{font-size:8px;color:var(--muted,#68756c)}.pv-debt-row .money{font-size:9.5px;font-weight:800;white-space:nowrap}.pv-debt-row .rate{font-size:9px}.pv-risk{font-size:8px;font-weight:850;text-align:center;border-radius:999px;padding:4px 5px;background:#fff0dc;color:#a96512}.pv-risk.high{background:#fde7e5;color:#b74439}.pv-risk.low{background:#e7f4ea;color:#2b7d45}
    .pv-selected{display:grid;gap:7px}.pv-sel-top{display:flex;align-items:flex-start;gap:8px}.pv-sel-icon{width:34px;height:34px;border-radius:10px;display:grid;place-items:center;background:var(--panel,#fff);font-size:15px}.pv-sel-name{min-width:0}.pv-sel-name b{font-size:11px}.pv-sel-name small{display:block;color:var(--muted,#68756c);font-size:8px;margin-top:2px}.pv-big{font:800 20px/1 Georgia,serif;margin:2px 0}.pv-mini-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:5px}.pv-mini{padding:6px;border-radius:8px;background:var(--panel,#fff)}.pv-mini span{display:block;font-size:7.5px;color:var(--muted,#68756c)}.pv-mini b{display:block;font-size:9px;margin-top:2px}.pv-progress{height:7px;background:var(--border,#dfe6e0);border-radius:99px;overflow:hidden}.pv-progress i{display:block;height:100%;background:var(--brand,#276340);border-radius:inherit}.pv-note{padding:7px 8px;border-radius:8px;background:var(--panel,#fff);font-size:8.5px;line-height:1.35;color:var(--muted,#68756c)}
    .pv-sim{height:100%;display:grid;grid-template-columns:270px minmax(0,1fr);gap:8px}.pv-controls{display:grid;align-content:start;gap:7px}.pv-field{display:grid;gap:3px}.pv-field label{font-size:8.5px;color:var(--muted,#68756c);font-weight:750}.pv-field input,.pv-field select{width:100%;height:31px;border:1px solid var(--border,#dfe6e0);border-radius:8px;background:var(--panel,#fff);color:inherit;padding:0 8px;font-size:10px;outline:0}.pv-field input:focus,.pv-field select:focus{border-color:var(--brand,#276340)}.pv-range{display:grid;grid-template-columns:1fr 72px;gap:5px;align-items:center}.pv-range input[type=range]{padding:0;border:0;background:transparent}.pv-sim-right{display:grid;grid-template-rows:auto minmax(0,1fr);gap:8px;min-height:0}.pv-result-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px}.pv-result{padding:7px;border-radius:9px;background:var(--panel2,#f5f7f4);border:1px solid var(--border,#dfe6e0);min-width:0}.pv-result span{display:block;color:var(--muted,#68756c);font-size:7.5px}.pv-result b{display:block;font:800 13px/1.05 Georgia,serif;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pv-result.good b{color:#2f7f49}.pv-chart{min-height:0;background:var(--panel2,#f5f7f4);border:1px solid var(--border,#dfe6e0);border-radius:10px;padding:7px;display:grid;grid-template-rows:auto minmax(0,1fr)}.pv-chart-head{display:flex;align-items:center;justify-content:space-between;font-size:8px;color:var(--muted,#68756c)}.pv-chart svg{width:100%;height:100%;min-height:140px}.pv-chart-grid{stroke:var(--border,#dfe6e0);stroke-width:1}.pv-chart-base{fill:none;stroke:#98a69b;stroke-width:2}.pv-chart-plan{fill:none;stroke:var(--brand,#276340);stroke-width:2.4}.pv-chart-area{fill:color-mix(in srgb,var(--brand,#276340) 10%,transparent)}
    .pv-calc-layout{height:100%;display:grid;grid-template-columns:175px minmax(0,1fr);gap:8px}.pv-calc-nav{display:grid;align-content:start;gap:4px}.pv-calc-btn{height:31px;text-align:left;border:1px solid transparent;border-radius:8px;background:transparent;color:inherit;padding:0 8px;font-size:9px;font-weight:750;cursor:pointer}.pv-calc-btn:hover{background:var(--panel,#fff)}.pv-calc-btn.active{background:var(--panel,#fff);border-color:var(--border,#dfe6e0)}.pv-calc-main{min-height:0;display:grid;grid-template-columns:minmax(245px,.7fr) minmax(0,1fr);gap:8px}.pv-formbox,.pv-outputbox{background:var(--panel2,#f5f7f4);border:1px solid var(--border,#dfe6e0);border-radius:10px;padding:9px;min-height:0;overflow:auto}.pv-formbox h3,.pv-outputbox h3{margin:0 0 7px;font-size:11px}.pv-formgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}.pv-output-hero{font:800 23px/1 Georgia,serif;margin:5px 0}.pv-output-list{display:grid;gap:5px}.pv-output-row{display:flex;align-items:center;justify-content:space-between;gap:8px;padding:7px;background:var(--panel,#fff);border-radius:8px;font-size:8.5px}.pv-output-row b{font-size:9.5px}.pv-disclaimer{margin-top:7px;font-size:7.8px;line-height:1.35;color:var(--muted,#68756c)}
    .pv-strategy{height:100%;display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:8px}.pv-strategy-card{background:var(--panel2,#f5f7f4);border:1px solid var(--border,#dfe6e0);border-radius:10px;padding:9px;overflow:auto}.pv-strategy-card.winner{border-color:color-mix(in srgb,var(--brand,#276340) 45%,transparent)}.pv-strategy-card h3{margin:0;font-size:12px}.pv-strategy-sub{font-size:8px;color:var(--muted,#68756c);margin:2px 0 8px}.pv-order{display:grid;gap:5px}.pv-order-row{display:grid;grid-template-columns:22px minmax(0,1fr) auto;gap:6px;align-items:center;padding:6px 7px;border-radius:8px;background:var(--panel,#fff);font-size:8.5px}.pv-order-row i{width:20px;height:20px;border-radius:7px;background:var(--panel2,#eef3ed);display:grid;place-items:center;font-style:normal;font-weight:850}.pv-strategy-stats{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:5px;margin-top:7px}.pv-strategy-stats div{background:var(--panel,#fff);border-radius:8px;padding:6px}.pv-strategy-stats span{display:block;font-size:7px;color:var(--muted,#68756c)}.pv-strategy-stats b{display:block;margin-top:2px;font-size:9px}.pv-insight{margin-top:7px;padding:7px;border-radius:8px;background:color-mix(in srgb,var(--brand,#276340) 8%,var(--panel,#fff));font-size:8px;line-height:1.35}
    .pv-modal-shade{position:fixed;inset:0;z-index:9998;background:#0a17106e;display:grid;place-items:center;padding:20px}.pv-modal-card{width:min(520px,calc(100vw - 30px));max-height:calc(100vh - 40px);overflow:auto;background:var(--panel,#fff);color:var(--text,#17301f);border-radius:14px;padding:14px;border:1px solid var(--border,#dfe6e0);box-shadow:0 20px 60px #0004}.pv-modal-card h3{margin:0 0 10px}.pv-modal-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px}.pv-modal-actions{display:flex;justify-content:flex-end;gap:7px;margin-top:10px}
    @media(max-width:900px){.pv-debt-kpis{grid-template-columns:repeat(3,minmax(0,1fr))}.pv-dgrid2,.pv-sim,.pv-calc-main,.pv-strategy{grid-template-columns:1fr}.pv-calc-layout{grid-template-columns:1fr}.pv-calc-nav{grid-template-columns:repeat(3,minmax(0,1fr))}.pv-debt-row{grid-template-columns:minmax(0,1fr) .8fr .7fr 58px}.pv-debt-row .rate{display:none}}
  `;
  document.head.appendChild(style);

  function loadDebts(){try{const x=JSON.parse(localStorage.getItem(STORAGE)||'null');if(Array.isArray(x)&&x.length)return x}catch(e){}localStorage.setItem(STORAGE,JSON.stringify(DEFAULT_DEBTS));return structuredClone(DEFAULT_DEBTS)}
  function saveDebts(){localStorage.setItem(STORAGE,JSON.stringify(debts))}
  function saveUi(){localStorage.setItem(UIKEY,JSON.stringify(state))}
  function num(v,d=0){v=Number(v);return Number.isFinite(v)?v:d}
  function clamp(v,min,max){return Math.min(max,Math.max(min,v))}
  function currency(){const c=(window.state?.settings?.currency||window.S?.settings?.currency||document.querySelector('select[data-currency]')?.value||'INR').toUpperCase();return ['INR','USD','EUR','GBP','AED','CNY','JPY','RUB','CAD','AUD','SGD','CHF'].includes(c)?c:'INR'}
  function money(v){const c=currency();try{return new Intl.NumberFormat(c==='INR'?'en-IN':'en-US',{style:'currency',currency:c,maximumFractionDigits:0}).format(num(v))}catch(e){return (c==='INR'?'₹':'')+Math.round(num(v)).toLocaleString('en-IN')}}
  function selected(){return debts.find(d=>d.id===state.selected)||debts[0]||null}
  function monthlyRate(apr){return Math.max(0,num(apr))/1200}
  function emi(principal,apr,months){principal=Math.max(0,num(principal));months=Math.max(1,Math.round(num(months,1)));const r=monthlyRate(apr);if(!r)return principal/months;const p=Math.pow(1+r,months);return principal*r*p/(p-1)}
  function amortize(balance,apr,payment,lump=0,maxMonths=1200){balance=Math.max(0,num(balance)-Math.max(0,num(lump)));payment=Math.max(0,num(payment));const r=monthlyRate(apr);let b=balance,interest=0,m=0;const pts=[balance];if(b<=0)return{months:0,interest:0,total:0,points:[0]};if(payment<=0||payment<=b*r)return{months:Infinity,interest:Infinity,total:Infinity,points:[balance]};while(b>.01&&m<maxMonths){const i=b*r;interest+=i;b=Math.max(0,b+i-payment);m++;if(m%3===0||b===0)pts.push(b)}return{months:m>=maxMonths?Infinity:m,interest,total:balance+interest,points:pts}}
  function risk(d){return d.apr>=24?['High','high']:d.apr>=12?['Watch','']:['Low','low']}
  function totalBalance(){return debts.reduce((s,d)=>s+num(d.balance),0)}
  function totalPayment(){return debts.reduce((s,d)=>s+Math.max(0,num(d.payment||d.minPayment)),0)}
  function weightedApr(){const b=totalBalance();return b?debts.reduce((s,d)=>s+num(d.balance)*num(d.apr),0)/b:0}
  function allPayoff(){let months=0,interest=0;for(const d of debts){const a=amortize(d.balance,d.apr,d.payment||d.minPayment);if(Number.isFinite(a.months))months=Math.max(months,a.months);else months=Infinity;if(Number.isFinite(a.interest))interest+=a.interest;else interest=Infinity}return{months,interest}}
  function monthsText(m){if(!Number.isFinite(m))return'Payment too low';if(m===0)return'Paid';const y=Math.floor(m/12),mo=m%12;return y?`${y}y ${mo}m`:`${mo} months`}
  function header(){const a=allPayoff();return `<div class="pv-debt-head"><div class="pv-debt-title"><h2>Debt Control Center</h2><p>Track every debt, simulate payoff changes, compare strategies and turn calculations into a plan.</p></div><div class="pv-debt-head-actions"><button class="pv-dbtn soft" data-dtab="simulator">Payoff Simulator</button><button class="pv-dbtn primary" data-debt-add>＋ Add Debt</button></div></div><div class="pv-debt-kpis"><div class="pv-dkpi"><span>Total Debt</span><b>${money(totalBalance())}</b><small>${debts.length} active account${debts.length===1?'':'s'}</small></div><div class="pv-dkpi"><span>Monthly Payments</span><b>${money(totalPayment())}</b><small>Current planned outflow</small></div><div class="pv-dkpi"><span>Weighted APR</span><b>${weightedApr().toFixed(1)}%</b><small>Across balances</small></div><div class="pv-dkpi"><span>Debt-Free Estimate</span><b>${monthsText(a.months)}</b><small>At current payments</small></div><div class="pv-dkpi"><span>Projected Interest</span><b>${Number.isFinite(a.interest)?money(a.interest):'—'}</b><small>Current plan estimate</small></div></div>`}
  function tabs(){return `<div class="pv-dtabs">${[['overview','Overview'],['simulator','Payoff Simulator'],['calculators','Calculators'],['strategies','Snowball vs Avalanche']].map(([k,l])=>`<button class="pv-dtab ${state.tab===k?'active':''}" data-dtab="${k}">${l}</button>`).join('')}</div>`}
  function overview(){const d=selected();if(!d)return `<div class="pv-dpane"><div class="pv-note">No debts yet. Use <b>Add Debt</b> to create your first debt record.</div></div>`;const pay=Math.max(num(d.payment),num(d.minPayment)),a=amortize(d.balance,d.apr,pay),[rt,rc]=risk(d),initial=DEFAULT_DEBTS.find(x=>x.name===d.name)?.balance||d.balance,progress=clamp((1-d.balance/Math.max(initial,d.balance))*100,0,100);return `<div class="pv-dpane"><div class="pv-dgrid2"><section class="pv-dcard"><h3>Your Debts</h3><div class="pv-debt-list">${debts.map(x=>{const [t,c]=risk(x);return `<div class="pv-debt-row ${x.id===d.id?'active':''}" data-debt-select="${x.id}"><div class="name"><b>${esc(x.name)}</b><small>${esc(x.type)}</small></div><div><small>Balance</small><div class="money">${money(x.balance)}</div></div><div class="rate"><small>APR</small><div>${num(x.apr).toFixed(1)}%</div></div><div><small>Payment</small><div class="money">${money(x.payment||x.minPayment)}</div></div><div class="pv-risk ${c}">${t}</div></div>`}).join('')}</div></section><aside class="pv-dcard"><div class="pv-selected"><div class="pv-sel-top"><div class="pv-sel-icon">◇</div><div class="pv-sel-name"><b>${esc(d.name)}</b><small>${esc(d.type)} · Due day ${d.dueDay||'—'}</small></div><button class="pv-dbtn" style="margin-left:auto" data-debt-edit="${d.id}">Edit</button></div><div><small style="color:var(--muted)">Remaining balance</small><div class="pv-big">${money(d.balance)}</div></div><div class="pv-mini-grid"><div class="pv-mini"><span>APR</span><b>${num(d.apr).toFixed(2)}%</b></div><div class="pv-mini"><span>Monthly payment</span><b>${money(pay)}</b></div><div class="pv-mini"><span>Est. payoff</span><b>${monthsText(a.months)}</b></div><div class="pv-mini"><span>Est. interest</span><b>${Number.isFinite(a.interest)?money(a.interest):'—'}</b></div></div><div><div style="display:flex;justify-content:space-between;font-size:8px;margin-bottom:3px"><span>Payoff progress</span><b>${Math.round(progress)}%</b></div><div class="pv-progress"><i style="width:${progress}%"></i></div></div><button class="pv-dbtn primary" data-dtab="simulator">Simulate faster payoff</button><div class="pv-note"><b>${rt==='High'?'High-interest priority':'Plan insight'}:</b> ${rt==='High'?'This debt has a high APR. Compare Avalanche to see the potential interest advantage.':'Try an extra monthly amount or one-time prepayment to measure the time and interest saved.'}</div></div></aside></div></div>`}
  function chartSvg(basePts,planPts){const W=720,H=230,pad=20,maxLen=Math.max(basePts.length,planPts.length,2),maxV=Math.max(...basePts,...planPts,1),path=pts=>pts.map((v,i)=>`${i?'L':'M'} ${(pad+i*(W-pad*2)/Math.max(1,maxLen-1)).toFixed(1)} ${(H-pad-v/maxV*(H-pad*2)).toFixed(1)}`).join(' '),pp=path(planPts);return `<svg viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" aria-label="Debt balance payoff chart"><line class="pv-chart-grid" x1="${pad}" y1="${H-pad}" x2="${W-pad}" y2="${H-pad}"/><line class="pv-chart-grid" x1="${pad}" y1="${pad}" x2="${pad}" y2="${H-pad}"/><path class="pv-chart-base" d="${path(basePts)}"/><path class="pv-chart-area" d="${pp} L ${W-pad} ${H-pad} L ${pad} ${H-pad} Z"/><path class="pv-chart-plan" d="${pp}"/></svg>`}
  function simulator(){const d=selected();if(!d)return `<div class="pv-dpane">Add a debt first.</div>`;const pay=Math.max(1,num(d.payment||d.minPayment)),base=amortize(d.balance,d.apr,pay),plan=amortize(d.balance,d.apr,pay+num(state.extra),num(state.lump)),savedM=Number.isFinite(base.months)&&Number.isFinite(plan.months)?Math.max(0,base.months-plan.months):0,savedI=Number.isFinite(base.interest)&&Number.isFinite(plan.interest)?Math.max(0,base.interest-plan.interest):0;return `<div class="pv-dpane"><div class="pv-sim"><section class="pv-dcard pv-controls"><h3>Payoff inputs</h3><div class="pv-field"><label>Debt</label><select data-sim-debt>${debts.map(x=>`<option value="${x.id}" ${x.id===d.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="pv-field"><label>Current balance</label><input value="${money(d.balance)}" disabled></div><div class="pv-field"><label>APR</label><input value="${num(d.apr).toFixed(2)}%" disabled></div><div class="pv-field"><label>Current monthly payment</label><input value="${money(pay)}" disabled></div><div class="pv-field"><label>Extra every month</label><div class="pv-range"><input type="range" min="0" max="${Math.max(20000,Math.round(pay*2))}" step="500" value="${num(state.extra)}" data-extra-range><input type="number" min="0" step="500" value="${num(state.extra)}" data-extra></div></div><div class="pv-field"><label>One-time prepayment now</label><input type="number" min="0" step="1000" max="${d.balance}" value="${num(state.lump)}" data-lump></div><button class="pv-dbtn primary" data-apply-plan>Apply strategy to debt plan</button><div class="pv-note">Calculations run locally in your browser. Results are estimates based on a fixed APR and monthly payment.</div></section><section class="pv-sim-right"><div class="pv-result-grid"><div class="pv-result"><span>New monthly payment</span><b>${money(pay+num(state.extra))}</b></div><div class="pv-result"><span>New payoff</span><b>${monthsText(plan.months)}</b></div><div class="pv-result good"><span>Time saved</span><b>${savedM?monthsText(savedM):'0 months'}</b></div><div class="pv-result good"><span>Interest saved</span><b>${Number.isFinite(savedI)?money(savedI):'—'}</b></div></div><div class="pv-chart"><div class="pv-chart-head"><span>Remaining balance projection</span><span>Gray: current · Green: new plan</span></div>${chartSvg(base.points,plan.points)}</div></section></div></div>`}
  const CALCS=[['emi','EMI / Loan'],['simple','Simple Interest'],['compound','Compound Interest'],['card','Credit Card Payoff'],['refi','Refinance Compare']];
  function calculators(){return `<div class="pv-dpane"><div class="pv-calc-layout"><aside class="pv-dcard pv-calc-nav">${CALCS.map(([k,l])=>`<button class="pv-calc-btn ${state.calc===k?'active':''}" data-calc="${k}">${l}</button>`).join('')}</aside><div id="pvCalcMain">${calcPanel()}</div></div></div>`}
  function calcPanel(){if(state.calc==='simple')return calcSimple();if(state.calc==='compound')return calcCompound();if(state.calc==='card')return calcCard();if(state.calc==='refi')return calcRefi();return calcEmi()}
  function f(name,label,value,type='number'){return `<div class="pv-field"><label>${label}</label><input data-cf="${name}" type="${type}" value="${value}"></div>`}
  function calcEmi(){const p=500000,r=9.5,m=60,e=emi(p,r,m),tot=e*m;return calcShell('EMI / Loan Calculator','Calculate the fixed monthly payment and repayment cost.',`${f('p','Loan amount',p)}${f('r','Annual interest %',r)}${f('m','Term (months)',m)}`,money(e),[['Monthly EMI',money(e)],['Total interest',money(tot-p)],['Total repayment',money(tot)]])}
  function calcSimple(){const p=100000,r=8,y=3,i=p*r*y/100;return calcShell('Simple Interest','For non-compounding interest calculations.',`${f('p','Principal',p)}${f('r','Annual interest %',r)}${f('y','Years',y)}`,money(p+i),[['Interest earned / paid',money(i)],['Principal',money(p)],['Final amount',money(p+i)]])}
  function calcCompound(){const p=100000,r=10,y=10,n=12,monthly=5000,factor=Math.pow(1+r/100/n,n*y),fv=p*factor+monthly*((factor-1)/(r/100/n)),invested=p+monthly*n*y;return calcShell('Compound Interest','Model compounding plus a recurring monthly contribution.',`${f('p','Starting amount',p)}${f('r','Annual return / rate %',r)}${f('y','Years',y)}${f('monthly','Monthly contribution',monthly)}`,money(fv),[['Total contributed',money(invested)],['Estimated growth',money(fv-invested)],['Estimated final value',money(fv)]])}
  function calcCard(){const b=80000,apr=34,pay=5000,a=amortize(b,apr,pay);return calcShell('Credit Card Payoff','See how a fixed payment changes payoff time and interest.',`${f('b','Card balance',b)}${f('apr','APR %',apr)}${f('pay','Monthly payment',pay)}`,monthsText(a.months),[['Payoff time',monthsText(a.months)],['Estimated interest',Number.isFinite(a.interest)?money(a.interest):'Payment too low'],['Total paid',Number.isFinite(a.total)?money(a.total):'—']])}
  function calcRefi(){const bal=800000,oldr=11.5,newr=9,months=48,fee=8000,old=emi(bal,oldr,months)*months,newTot=emi(bal,newr,months)*months+fee;return calcShell('Refinance Comparison','Compare a current loan with a refinance offer including fees.',`${f('bal','Balance',bal)}${f('oldr','Current APR %',oldr)}${f('newr','New APR %',newr)}${f('months','Remaining months',months)}${f('fee','Processing / refinance fee',fee)}`,money(Math.max(0,old-newTot)),[['Current remaining cost',money(old)],['Refinanced total cost',money(newTot)],['Potential saving',money(old-newTot)]])}
  function calcShell(title,desc,fields,hero,rows){return `<div class="pv-calc-main"><section class="pv-formbox"><h3>${title}</h3><p class="pv-strategy-sub">${desc}</p><div class="pv-formgrid">${fields}</div></section><section class="pv-outputbox"><h3>Result</h3><div class="pv-output-hero" data-calc-hero>${hero}</div><div class="pv-output-list">${rows.map((r,i)=>`<div class="pv-output-row"><span>${r[0]}</span><b data-calc-row="${i}">${r[1]}</b></div>`).join('')}</div><div class="pv-disclaimer">Educational estimate only. Rates, lender rules, taxes, fees and compounding conventions can change actual results.</div></section></div>`}
  function strategySimulation(order){const ds=debts.map(d=>({...d,balance:num(d.balance)}));let month=0,interest=0;const baseBudget=Math.max(totalPayment(),1),max=1200;let firstCleared=null;while(ds.some(d=>d.balance>.01)&&month<max){month++;let budget=baseBudget;for(const d of ds){if(d.balance<=.01)continue;const min=Math.min(d.balance*(1+monthlyRate(d.apr)),Math.max(1,num(d.minPayment)||Math.min(num(d.payment),d.balance))),int=d.balance*monthlyRate(d.apr);interest+=int;d.balance+=int;const pay=Math.min(d.balance,min);d.balance-=pay;budget-=pay;if(d.balance<=.01&&!firstCleared)firstCleared=month}const active=ds.filter(d=>d.balance>.01).sort(order);for(const d of active){if(budget<=.01)break;const pay=Math.min(d.balance,budget);d.balance-=pay;budget-=pay;if(d.balance<=.01&&!firstCleared)firstCleared=month}}return{months:month>=max?Infinity:month,interest,first:firstCleared||0}}
  function strategies(){const snowOrder=(a,b)=>a.balance-b.balance,avaOrder=(a,b)=>b.apr-a.apr,snow=strategySimulation(snowOrder),ava=strategySimulation(avaOrder),win=ava.interest<=snow.interest?'avalanche':'snowball',card=(kind,title,sub,order,res)=>`<section class="pv-strategy-card ${win===kind?'winner':''}"><div style="display:flex;align-items:center;gap:6px"><h3>${title}</h3>${win===kind?'<span class="pv-risk low">Lower projected interest</span>':''}</div><div class="pv-strategy-sub">${sub}</div><div class="pv-order">${[...debts].sort(order).map((d,i)=>`<div class="pv-order-row"><i>${i+1}</i><div><b>${esc(d.name)}</b><div style="color:var(--muted);font-size:7.5px">${money(d.balance)} · ${num(d.apr).toFixed(1)}% APR</div></div><b>${money(d.payment||d.minPayment)}/mo</b></div>`).join('')}</div><div class="pv-strategy-stats"><div><span>Debt-free</span><b>${monthsText(res.months)}</b></div><div><span>Projected interest</span><b>${money(res.interest)}</b></div><div><span>First payoff</span><b>${res.first?monthsText(res.first):'—'}</b></div></div><div class="pv-insight">${kind==='snowball'?'Prioritizes the smallest balance first, which can create faster visible wins.':'Prioritizes the highest APR first, normally targeting interest cost more aggressively.'}</div></section>`;return `<div class="pv-dpane"><div class="pv-strategy">${card('snowball','Debt Snowball','Smallest balance first.',snowOrder,snow)}${card('avalanche','Debt Avalanche','Highest interest rate first.',avaOrder,ava)}</div></div>`}
  function renderDebt(){const host=document.querySelector('.workspace')||document.querySelector('.content');if(!host||!isDebtView())return;host.innerHTML=`<div class="pv-debt">${header()}<div class="pv-debt-body">${tabs()}${state.tab==='overview'?overview():state.tab==='simulator'?simulator():state.tab==='calculators'?calculators():strategies()}</div></div>`;wire(host);updateTopTitle()}
  function isDebtView(){const active=[...document.querySelectorAll('.nav-btn.active,.nav.active,[data-nav].active')].find(b=>(b.dataset.nav||b.dataset.v||b.textContent||'').trim().toLowerCase().includes('debt'));if(active)return true;const t=(document.querySelector('#pvTop .pv-title')?.textContent||document.querySelector('.topbar')?.textContent||'').toLowerCase();return /\bdebt\b/.test(t)}
  function updateTopTitle(){const t=document.querySelector('#pvTop .pv-title');if(t)t.textContent='Debt Control Center'}
  function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
  function wire(root){root.querySelectorAll('[data-dtab]').forEach(b=>b.addEventListener('click',()=>{state.tab=b.dataset.dtab;saveUi();renderDebt()}));root.querySelectorAll('[data-debt-select]').forEach(b=>b.addEventListener('click',()=>{state.selected=b.dataset.debtSelect;saveUi();renderDebt()}));root.querySelectorAll('[data-debt-edit]').forEach(b=>b.addEventListener('click',e=>{e.stopPropagation();openDebtModal(debts.find(d=>d.id===b.dataset.debtEdit))}));root.querySelectorAll('[data-debt-add]').forEach(b=>b.addEventListener('click',()=>openDebtModal()));const sd=root.querySelector('[data-sim-debt]');if(sd)sd.addEventListener('change',()=>{state.selected=sd.value;state.extra=0;state.lump=0;saveUi();renderDebt()});const er=root.querySelector('[data-extra-range]'),ei=root.querySelector('[data-extra]'),syncExtra=v=>{state.extra=Math.max(0,num(v));saveUi();renderDebt()};if(er)er.addEventListener('input',()=>{if(ei)ei.value=er.value});if(er)er.addEventListener('change',()=>syncExtra(er.value));if(ei)ei.addEventListener('change',()=>syncExtra(ei.value));const lump=root.querySelector('[data-lump]');if(lump)lump.addEventListener('change',()=>{state.lump=Math.max(0,num(lump.value));saveUi();renderDebt()});const apply=root.querySelector('[data-apply-plan]');if(apply)apply.addEventListener('click',()=>{const d=selected();if(!d)return;d.payment=Math.max(1,num(d.payment||d.minPayment)+num(state.extra));if(num(state.lump)>0)d.balance=Math.max(0,num(d.balance)-num(state.lump));saveDebts();state.extra=0;state.lump=0;saveUi();toast('Payoff strategy applied to this debt');renderDebt()});root.querySelectorAll('[data-calc]').forEach(b=>b.addEventListener('click',()=>{state.calc=b.dataset.calc;saveUi();renderDebt()}));root.querySelectorAll('[data-cf]').forEach(i=>i.addEventListener('input',()=>recalc(root)))}
  function recalc(root){const get=n=>num(root.querySelector(`[data-cf="${n}"]`)?.value);let hero='',rows=[];if(state.calc==='simple'){const p=get('p'),r=get('r'),y=get('y'),it=p*r*y/100;hero=money(p+it);rows=[money(it),money(p),money(p+it)]}else if(state.calc==='compound'){const p=get('p'),r=get('r'),y=get('y'),monthly=get('monthly'),n=12,rr=r/100/n,factor=rr?Math.pow(1+rr,n*y):1,fv=p*factor+(rr?monthly*((factor-1)/rr):monthly*n*y),inv=p+monthly*n*y;hero=money(fv);rows=[money(inv),money(fv-inv),money(fv)]}else if(state.calc==='card'){const a=amortize(get('b'),get('apr'),get('pay'));hero=monthsText(a.months);rows=[monthsText(a.months),Number.isFinite(a.interest)?money(a.interest):'Payment too low',Number.isFinite(a.total)?money(a.total):'—']}else if(state.calc==='refi'){const bal=get('bal'),oldr=get('oldr'),newr=get('newr'),months=Math.max(1,get('months')),fee=get('fee'),old=emi(bal,oldr,months)*months,newTot=emi(bal,newr,months)*months+fee;hero=money(Math.max(0,old-newTot));rows=[money(old),money(newTot),money(old-newTot)]}else{const p=get('p'),r=get('r'),m=Math.max(1,get('m')),e=emi(p,r,m),tot=e*m;hero=money(e);rows=[money(e),money(tot-p),money(tot)]}const h=root.querySelector('[data-calc-hero]');if(h)h.textContent=hero;rows.forEach((v,i)=>{const el=root.querySelector(`[data-calc-row="${i}"]`);if(el)el.textContent=v})}
  function openDebtModal(d){const edit=!!d;d=d||{name:'',type:'Personal Loan',balance:0,apr:0,minPayment:0,payment:0,dueDay:1};const shade=document.createElement('div');shade.className='pv-modal-shade';shade.innerHTML=`<div class="pv-modal-card"><h3>${edit?'Edit Debt':'Add Debt'}</h3><div class="pv-modal-grid">${modalField('name','Debt name',d.name,'text')}${modalSelect('type','Type',d.type,['Credit Card','Personal Loan','Vehicle Loan','Home Loan','Education Loan','Other'])}${modalField('balance','Current balance',d.balance)}${modalField('apr','APR %',d.apr)}${modalField('payment','Monthly payment',d.payment||d.minPayment)}${modalField('dueDay','Due day',d.dueDay||1)}</div><div class="pv-modal-actions">${edit?'<button class="pv-dbtn" data-delete>Delete</button>':''}<button class="pv-dbtn" data-cancel>Cancel</button><button class="pv-dbtn primary" data-save>Save</button></div></div>`;document.body.appendChild(shade);shade.querySelector('[data-cancel]').onclick=()=>shade.remove();shade.addEventListener('click',e=>{if(e.target===shade)shade.remove()});if(edit)shade.querySelector('[data-delete]').onclick=()=>{debts=debts.filter(x=>x.id!==d.id);if(state.selected===d.id)state.selected=debts[0]?.id||'';saveDebts();saveUi();shade.remove();renderDebt();toast('Debt removed')};shade.querySelector('[data-save]').onclick=()=>{const val=n=>shade.querySelector(`[data-mf="${n}"]`)?.value,obj={id:edit?d.id:'db'+Date.now(),name:(val('name')||'Debt').trim(),type:val('type')||'Other',balance:Math.max(0,num(val('balance'))),apr:clamp(num(val('apr')),0,200),minPayment:0,payment:Math.max(0,num(val('payment'))),dueDay:clamp(Math.round(num(val('dueDay'),1)),1,31)};if(edit){const i=debts.findIndex(x=>x.id===d.id);debts[i]=obj}else{debts.push(obj);state.selected=obj.id}saveDebts();saveUi();shade.remove();renderDebt();toast(edit?'Debt updated':'Debt added')}}
  function modalField(n,l,v,t='number'){return `<div class="pv-field"><label>${l}</label><input data-mf="${n}" type="${t}" value="${esc(v)}"></div>`}
  function modalSelect(n,l,v,opts){return `<div class="pv-field"><label>${l}</label><select data-mf="${n}">${opts.map(x=>`<option ${x===v?'selected':''}>${x}</option>`).join('')}</select></div>`}
  function toast(msg){const existing=document.querySelector('.toast');if(existing&&typeof existing.textContent!=='undefined'){existing.textContent=msg;existing.classList.add('show','on');setTimeout(()=>existing.classList.remove('show','on'),1800);return}const t=document.createElement('div');t.textContent=msg;Object.assign(t.style,{position:'fixed',right:'20px',bottom:'20px',zIndex:10000,background:'#163d2a',color:'#fff',padding:'9px 12px',borderRadius:'9px',font:'600 11px system-ui'});document.body.appendChild(t);setTimeout(()=>t.remove(),1800)}
  let queued=false;const schedule=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;if(isDebtView()&&!document.querySelector('.pv-debt'))renderDebt()})};document.addEventListener('click',e=>{const b=e.target.closest('.nav-btn,.nav,[data-nav],[data-v]');if(!b)return;const label=(b.dataset.nav||b.dataset.v||b.textContent||'').trim().toLowerCase();if(label.includes('debt'))setTimeout(renderDebt,0)},true);const mo=new MutationObserver(schedule);mo.observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});setTimeout(schedule,80);
})();